type Query {
    getAllUsers: [User!]!
}

type Mutation {
    login(email: String!, password: String!): Token
}

type User {
    id: ID!
    email: String
    password: String
}

type Token {
    token: String
}
